'use strict';

/**
 * subscribe-section service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::subscribe-section.subscribe-section');
